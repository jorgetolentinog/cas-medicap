{
  "name": "cas-backend-medicap-sync",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx serverless package",
    "deploy": "npx serverless deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "serverless": "^3.22.0",
    "serverless-plugin-typescript": "^2.1.3",
    "typescript": "^4.8.4"
  }
}
