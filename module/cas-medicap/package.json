{
  "name": "cas-medicap",
  "version": "0.0.0",
  "scripts": {
    "deploy": "npx serverless deploy"
  },
  "dependencies": {
    "fast-safe-stringify": "^2.1.1"
  },
  "devDependencies": {
    "serverless": "^3.22.0"
  }
}
